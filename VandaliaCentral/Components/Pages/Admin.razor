@page "/admin"
@using VandaliaCentral.Services
@using VandaliaCentral.Models
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.Identity.Client
@using Microsoft.Identity.Web
@using Microsoft.JSInterop
@inject IJSRuntime JS
@inject ItNewsService ItNewsService
@inject ItProjectTrackerService ItProjectTrackerService
@inject LoggingService Logger
@inject GraphUserService GraphUserService
@inject AuthenticationStateProvider AuthStateProvider
@inject NavigationManager Navigation
@inject ITokenAcquisition TokenAcquisition
@inject MicrosoftIdentityConsentAndConditionalAccessHandler ConsentHandler
@inject IPasswordGeneratorService PasswordGenerator
@inject FlappyLeaderboardService FlappyLeaderboardService
@inject SowDocumentService SowDocumentService
@inject KnowledgeBaseService KnowledgeBaseService
@inject EmailRoutingSettingsService EmailRoutingSettingsService
@implements IAsyncDisposable


<div class="container-fluid border rounded bg-white shadow-sm col-md-12 py-4 admin-dashboard-shell">
    <div class="d-flex flex-wrap justify-content-between align-items-start gap-3 pb-4 border-bottom mb-4">
        <div>
            <h3 class="mb-1">Admin Dashboard</h3>
        </div>

    @if (!isAuthorized)
    {

        <p class="text-danger">Refresh the page, or please wait while we check your authorizations...</p>
    }
    else
    {
        <div class="ms-auto">
            <NavLink href="https://vandaliarental.targit.cloud/#vfs://global/Service/R%20Status%20Priority.xview" class="btn btn-dark" activeClass="active" target="_blank">

                @(isMobile ? "Targit Dashboard" : "My Targit Dashboard")
            </NavLink>
        </div>
    }
    </div>

    @if (isAuthorized)
    {
        <div class="admin-layout">
            <aside class="admin-nav-panel border rounded bg-light-subtle p-3">
                <h6 class="text-uppercase text-muted small mb-3">Admin sections</h6>

                <div class="admin-tab-grid" id="adminTabs" role="tablist">
                    <button class='btn text-start admin-tab-button @(IsActiveTab("logs"))' @onclick='() => SetActiveTab("logs")' type='button'>
                        <span class="fw-semibold d-block">Logs</span>
                        <small class="text-muted">Review system logs quickly</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("calendar"))' @onclick='() => SetActiveTab("calendar")' type='button'>
                        <span class="fw-semibold d-block">Company Calendar</span>
                        <small class="text-muted">Publish internal calendar events</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("itnews"))' @onclick='() => SetActiveTab("itnews")' type='button'>
                        <span class="fw-semibold d-block">IT News</span>
                        <small class="text-muted">Post and edit IT announcements</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("sowdocs"))' @onclick='() => SetActiveTab("sowdocs")' type='button'>
                        <span class="fw-semibold d-block">SOW Documents</span>
                        <small class="text-muted">Upload and curate SOW PDFs</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("mondayminute"))' @onclick='() => SetActiveTab("mondayminute")' type='button'>
                        <span class="fw-semibold d-block">Monday Minute</span>
                        <small class="text-muted">Access Monday Minute manager</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("passwords"))' @onclick='() => SetActiveTab("passwords")' type='button'>
                        <span class="fw-semibold d-block">Password Generator</span>
                        <small class="text-muted">Generate quick secure passwords</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("projecttracker"))' @onclick='() => SetActiveTab("projecttracker")' type='button'>
                        <span class="fw-semibold d-block">IT Project Tracker</span>
                        <small class="text-muted">Track project milestones</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("knowledgebase"))' @onclick='() => SetActiveTab("knowledgebase")' type='button'>
                        <span class="fw-semibold d-block">Knowledge Base</span>
                        <small class="text-muted">Manage folders and articles</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("emails"))' @onclick='() => SetActiveTab("emails")' type='button'>
                        <span class="fw-semibold d-block">Email Routing</span>
                        <small class="text-muted">Update form and notification recipients</small>
                    </button>
                    <button class='btn text-start admin-tab-button @(IsActiveTab("flappybird"))' @onclick='() => SetActiveTab("flappybird")' type='button'>
                        <span class="fw-semibold d-block">Flappy Bird</span>
                        <small class="text-muted">Review challenge leaderboards</small>
                    </button>
                </div>
            </aside>

            <section class="admin-content-panel border rounded p-3 p-md-4">
                @if (!string.IsNullOrWhiteSpace(GetTabHeading(activeTab)))
                {
                    <h5 class="mb-4">@GetTabHeading(activeTab)</h5>
                }




        @if (activeTab == "logs")
        {
            <div>
                <h5>Log Viewer</h5>

                @if (logFiles == null)
                {
                    <p>Loading logs...</p>
                }
                else
                {
                    <select @onchange="OnLogSelected" class="form-select w-25 mb-3">
                        <option disabled selected value="">-- Select Log File --</option>
                        @foreach (var file in logFiles)
                        {
                            <option value="@file">@file</option>
                        }
                    </select>

                    @if (!string.IsNullOrEmpty(logContent))
                    {
                        <div style="max-height: 400px; overflow-y: auto;">
                            <pre class="bg-light p-3 border rounded" style="white-space: pre-wrap;">@logContent</pre>
                        </div>
                    }
                }
            </div>
        }

        @if (activeTab == "calendar")
        {
            <CompanyCalendarManager />
        }

        @if (activeTab == "itnews")
        {
            <div>
                        <h5>IT News Manager</h5>
                <div class="border rounded p-3 mb-4" style="max-width: 700px;">
                    
                    <h6>Add IT News Item</h6>
                    <label class="mt-2">Title:</label>
                    <InputText @bind-Value="newItNewsTitle" class="form-control" />

                    <label class="mt-2">News Content:</label>
                    <InputTextArea @bind-Value="newItNewsContent" rows="4" class="form-control" />

                    <label class="mt-2">Expiration Date (optional):</label>
                    <InputDate @bind-Value="newItNewsExpirationDate" TValue="DateTime?" class="form-control" />

                    <div class="form-check form-switch mt-3">
                        <InputCheckbox id="newItNewsArchivable" @bind-Value="newItNewsArchivable" class="form-check-input" />
                        <label class="form-check-label" for="newItNewsArchivable">Archivable: @(newItNewsArchivable ? "Yes" : "No")</label>
                    </div>

                    <button class="btn btn-success mt-3" @onclick="AddNewsItem">Add News Item</button>
                </div>

                @if (itNewsItems == null)
                {
                    <p>Loading IT news...</p>
                }
                else if (!itNewsItems.Any())
                {
                    <p class="text-muted">No IT news items yet.</p>
                }
                else
                {
                    <ul class="list-group">
                        @foreach (var item in itNewsItems)
                        {
                            <li class="list-group-item">
                                @if (editingItNewsId == item.Id)
                                {
                                    <label>Title:</label>
                                    <InputText @bind-Value="editItNewsTitle" class="form-control mb-2" />

                                    <label>News Content:</label>
                                    <InputTextArea @bind-Value="editItNewsContent" rows="4" class="form-control" />

                                    <label class="mt-2">Expiration Date (optional):</label>
                                    <InputDate @bind-Value="editItNewsExpirationDate" TValue="DateTime?" class="form-control" />

                                    <div class="form-check form-switch mt-3">
                                        <InputCheckbox id="@($"editItNewsArchivable-{item.Id}")" @bind-Value="editItNewsArchivable" class="form-check-input" />
                                        <label class="form-check-label" for="@($"editItNewsArchivable-{item.Id}")">Archivable: @(editItNewsArchivable ? "Yes" : "No")</label>
                                    </div>

                                    <div class="d-flex gap-2 mt-3">
                                        <button class="btn btn-sm btn-primary" @onclick="SaveEditedNewsItem">Save</button>
                                        <button class="btn btn-sm btn-secondary" @onclick="CancelEditNewsItem">Cancel</button>
                                    </div>
                                }
                                else
                                {
                                    <div class="d-flex justify-content-between align-items-start gap-3">
                                        <div>
                                            <strong>@item.Title</strong>
                                            <div class="text-muted small">@item.PostedOn.ToLocalTime().ToString("MMM d, yyyy")</div>
                                            @if (item.ExpirationDate.HasValue)
                                            {
                                                <div class="text-muted small">Expires: @item.ExpirationDate.Value.ToString("MMM d, yyyy")</div>
                                            }
                                            <div class="text-muted small">Archivable: @(item.Archivable ? "Yes" : "No")</div>
                                            <div style="white-space: pre-wrap;">@item.Content</div>
                                        </div>
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-primary" @onclick="() => StartEditNewsItem(item)">Edit</button>
                                            <button class="btn btn-sm btn-danger" @onclick="() => DeleteNewsItem(item)">Delete</button>
                                        </div>
                                    </div>
                                }
                            </li>
                        }
                    </ul>
                }

                @if (showNewsSaveSuccess)
                {
                    <div class="text-success fw-bold mt-3">✓ IT news saved!</div>
                }
            </div>
        }

        @if (activeTab == "sowdocs")
        {
            <div>
                <h5>Scope of Work Documents</h5>
                <div class="pt-2 pb-3 d-flex flex-wrap gap-3 align-items-center">
                    <label class="btn btn-primary text-nowrap">
                        Upload SOW PDF
                        <InputFile OnChange="HandleSowFileUpload" accept=".pdf,application/pdf" class="d-none" />
                    </label>

                    @if (!string.IsNullOrWhiteSpace(sowUploadStatus))
                    {
                        <span class="badge bg-secondary fs-6">@sowUploadStatus</span>
                    }
                </div>

                @if (sowDocuments == null)
                {
                    <p>Loading SOW documents...</p>
                }
                else if (!sowDocuments.Any())
                {
                    <p class="text-muted">No SOW documents uploaded yet.</p>
                }
                else
                {
                    <ul class="list-group" style="max-width: 900px;">
                        @foreach (var document in sowDocuments)
                        {
                            <li class="list-group-item d-flex justify-content-between align-items-center gap-3">
                                <div>
                                    <div class="fw-semibold">@document.Title</div>
                                    <div class="text-muted small">@document.FileName</div>
                                </div>
                                <div class="d-flex gap-2">
                                    <a class="btn btn-sm btn-outline-primary" href="@document.Url" target="_blank" rel="noopener noreferrer">View</a>
                                    <button class="btn btn-sm btn-danger" @onclick="() => DeleteSowDocument(document)">Delete</button>
                                </div>
                            </li>
                        }
                    </ul>
                }
            </div>
        }

        @if (activeTab == "mondayminute")
        {
            <MondayMinuteManager />
        }

        @if (activeTab == "passwords")
        {
            <div>
                <h5>Password Generator</h5>
                <p class="text-muted mb-3">
                    2 words (≤ 5 chars each) + a number + a special character
                </p>

                <div class="d-flex align-items-center gap-2" style="max-width: 520px;">
                    <button class="btn btn-primary" @onclick="GeneratePassword">
                        Generate
                    </button>

                    <input class="form-control" value="@generatedPassword" readonly />

                    <button class="btn btn-outline-secondary"
                            @onclick="CopyPassword"
                            disabled="@string.IsNullOrWhiteSpace(generatedPassword)">
                        Copy
                    </button>
                </div>

                @if (showCopySuccess)
                {
                    <div class="text-success fw-bold mt-2">✓ Copied!</div>
                }
            </div>
        }

        @if (activeTab == "projecttracker")
        {
            <div>
                <h5>IT Project Tracker</h5>
                <p class="text-muted">Track your personal IT projects and contributions.</p>

                <div class="border rounded p-3 mb-4" style="max-width: 900px;">
                    <h6>Add Project Contribution</h6>
                    <label class="mt-2">Title:</label>
                    <InputText @bind-Value="newProjectTitle" class="form-control" />

                    <label class="mt-2">Notes:</label>
                    <InputTextArea @bind-Value="newProjectNotes" rows="4" class="form-control" />

                    <label class="mt-2">Completed Date:</label>
                    <InputDate @bind-Value="newProjectCompletedDate" class="form-control" />

                    <button class="btn btn-success mt-3" @onclick="AddProjectItem">Add Project</button>
                </div>

                @if (itProjectItems == null)
                {
                    <p>Loading your project entries...</p>
                }
                else
                {
                    <div class="mb-3">
                        <a class="btn btn-outline-primary"
                           href="/api/it-project-tracker/download"
                           download>
                            Download PDF
                        </a>
                    </div>

                    @if (!itProjectItems.Any())
                    {
                        <p class="text-muted">You do not have any project entries yet.</p>
                    }
                    else
                    {
                        <ul class="list-group" style="max-width: 900px;">
                        @foreach (var item in itProjectItems)
                        {
                            <li class="list-group-item">
                                @if (editingProjectId == item.Id)
                                {
                                    <label>Title:</label>
                                    <InputText @bind-Value="editProjectTitle" class="form-control mb-2" />

                                    <label>Notes:</label>
                                    <InputTextArea @bind-Value="editProjectNotes" rows="4" class="form-control mb-2" />

                                    <label>Completed Date:</label>
                                    <InputDate @bind-Value="editProjectCompletedDate" class="form-control" />

                                    <div class="d-flex gap-2 mt-3">
                                        <button class="btn btn-sm btn-primary" @onclick="SaveEditedProjectItem">Save</button>
                                        <button class="btn btn-sm btn-secondary" @onclick="CancelEditProjectItem">Cancel</button>
                                    </div>
                                }
                                else
                                {
                                    <div class="d-flex justify-content-between align-items-start gap-3">
                                        <div>
                                            <strong>@item.Title</strong>
                                            <div class="text-muted small">Completed: @(item.CompletedDate.HasValue ? item.CompletedDate.Value.ToString("MMM d, yyyy") : "Not set")</div>
                                            <div style="white-space: pre-wrap;">@item.Notes</div>
                                        </div>
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-primary" @onclick="() => StartEditProjectItem(item)">Edit</button>
                                            <button class="btn btn-sm btn-danger" @onclick="() => DeleteProjectItem(item)">Delete</button>
                                        </div>
                                    </div>
                                }
                            </li>
                        }
                        </ul>
                    }
                }

                @if (showProjectSaveSuccess)
                {
                    <div class="text-success fw-bold mt-3">✓ Project tracker saved!</div>
                }
            </div>
        }

        @if (activeTab == "knowledgebase")
        {
            <div>
                <h5>Knowledge Base Manager</h5>
                <div class="border rounded p-3 mb-4" style="max-width: 700px;">
                    <h6>Add Folder</h6>
                    <label class="mt-2">Folder Name:</label>
                    <InputText @bind-Value="newKnowledgeFolderName" class="form-control" />
                    <button class="btn btn-success mt-3" @onclick="AddKnowledgeFolder">Add Folder</button>
                </div>

                <div class="border rounded p-3 mb-4" style="max-width: 900px;">
                    <h6>Add Article</h6>
                    <label class="mt-2">Folder:</label>
                    <select @bind="selectedFolderForNewArticleId" class="form-select">
                        <option value="">-- Select Folder --</option>
                        @if (knowledgeBaseFolders != null)
                        {
                            @foreach (var folder in knowledgeBaseFolders)
                            {
                                <option value="@folder.Id.ToString()">@folder.Name</option>
                            }
                        }
                    </select>

                    <label class="mt-2">Article Title:</label>
                    <InputText @bind-Value="newKnowledgeArticleTitle" class="form-control" />

                    <label class="mt-2">Scribe Iframe Link:</label>
                    <InputText @bind-Value="newKnowledgeArticleEmbedUrl" class="form-control" />

                    <button class="btn btn-success mt-3" @onclick="AddKnowledgeArticle">Add Article</button>
                </div>

                @if (knowledgeBaseFolders == null)
                {
                    <p>Loading knowledge base...</p>
                }
                else if (!knowledgeBaseFolders.Any())
                {
                    <p class="text-muted">No folders have been created yet.</p>
                }
                else
                {
                    <div class="d-flex flex-column gap-3" style="max-width: 1000px;">
                        @foreach (var folder in knowledgeBaseFolders)
                        {
                            <div class="border rounded p-3">
                                @if (editingKnowledgeFolderId == folder.Id)
                                {
                                    <label>Folder Name:</label>
                                    <InputText @bind-Value="editKnowledgeFolderName" class="form-control" />
                                    <div class="d-flex gap-2 mt-3">
                                        <button class="btn btn-sm btn-primary" @onclick="SaveEditedKnowledgeFolder">Save</button>
                                        <button class="btn btn-sm btn-secondary" @onclick="CancelEditKnowledgeFolder">Cancel</button>
                                    </div>
                                }
                                else
                                {
                                    <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
                                        <h6 class="mb-0">@folder.Name</h6>
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-primary" @onclick="() => StartEditKnowledgeFolder(folder)">Edit Folder</button>
                                            <button class="btn btn-sm btn-danger" @onclick="() => DeleteKnowledgeFolder(folder)">Delete Folder</button>
                                        </div>
                                    </div>
                                }

                                @if (!folder.Articles.Any())
                                {
                                    <p class="text-muted mb-0">No articles in this folder.</p>
                                }
                                else
                                {
                                    <ul class="list-group">
                                        @foreach (var article in folder.Articles)
                                        {
                                            <li class="list-group-item">
                                                @if (editingKnowledgeArticleId == article.Id)
                                                {
                                                    <label>Article Title:</label>
                                                    <InputText @bind-Value="editKnowledgeArticleTitle" class="form-control mb-2" />

                                                    <label>Scribe Iframe Link:</label>
                                                    <InputText @bind-Value="editKnowledgeArticleEmbedUrl" class="form-control mb-2" />

                                                    <label>Folder:</label>
                                                    <select @bind="editKnowledgeArticleFolderId" class="form-select">
                                                        @if (knowledgeBaseFolders != null)
                                                        {
                                                            @foreach (var availableFolder in knowledgeBaseFolders)
                                                            {
                                                                <option value="@availableFolder.Id.ToString()">@availableFolder.Name</option>
                                                            }
                                                        }
                                                    </select>

                                                    <div class="d-flex gap-2 mt-3">
                                                        <button class="btn btn-sm btn-primary" @onclick="SaveEditedKnowledgeArticle">Save</button>
                                                        <button class="btn btn-sm btn-secondary" @onclick="CancelEditKnowledgeArticle">Cancel</button>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <div class="d-flex justify-content-between align-items-start gap-3">
                                                        <div>
                                                            <strong>@article.Title</strong>
                                                            <div class="text-muted small" style="word-break: break-all;">@article.EmbedUrl</div>
                                                        </div>
                                                        <div class="d-flex gap-2">
                                                            <button class="btn btn-sm btn-outline-primary" @onclick="() => StartEditKnowledgeArticle(folder, article)">Edit</button>
                                                            <button class="btn btn-sm btn-danger" @onclick="() => DeleteKnowledgeArticle(folder, article)">Delete</button>
                                                        </div>
                                                    </div>
                                                }
                                            </li>
                                        }
                                    </ul>
                                }
                            </div>
                        }
                    </div>
                }

                @if (!string.IsNullOrWhiteSpace(knowledgeBaseStatusMessage))
                {
                    <div class="text-success fw-bold mt-3">✓ @knowledgeBaseStatusMessage</div>
                }
            </div>
        }


        @if (activeTab == "emails")
        {
            <div style="max-width: 950px;">
                <h5>Email Routing</h5>
                <p class="text-muted">These addresses override appsettings defaults and are stored in Azure Blob storage.</p>

                <div class="d-flex flex-column gap-3">
                    <div class="border rounded p-3 bg-light-subtle">
                        <h6 class="mb-3">Employee Change Form</h6>
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">To</label>
                                <InputText @bind-Value="emailRoutingSettings.EmployeeChangeTo" class="form-control" />
                            </div>
                            <div class="col-12 col-md-6">
                                <label class="form-label">Cc</label>
                                <InputText @bind-Value="emailRoutingSettings.EmployeeChangeCc" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="border rounded p-3 bg-light-subtle">
                        <h6 class="mb-3">Employee Termination Form</h6>
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">To</label>
                                <InputText @bind-Value="emailRoutingSettings.EmployeeTerminationTo" class="form-control" />
                            </div>
                            <div class="col-12 col-md-6">
                                <label class="form-label">Cc</label>
                                <InputText @bind-Value="emailRoutingSettings.EmployeeTerminationCc" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="border rounded p-3 bg-light-subtle">
                        <h6 class="mb-3">Feedback Form</h6>
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">To</label>
                                <InputText @bind-Value="emailRoutingSettings.FeedbackTo" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="border rounded p-3 bg-light-subtle">
                        <h6 class="mb-3">AM Assignment Change Request</h6>
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">Open Contracts To</label>
                                <InputText @bind-Value="emailRoutingSettings.AmOpenContractsTo" class="form-control" />
                            </div>
                            <div class="col-12 col-md-6">
                                <label class="form-label">Open Contracts Cc</label>
                                <InputText @bind-Value="emailRoutingSettings.AmOpenContractsCc" class="form-control" />
                            </div>
                            <div class="col-12 col-md-6">
                                <label class="form-label">Standard To</label>
                                <InputText @bind-Value="emailRoutingSettings.AmStandardTo" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="border rounded p-3 bg-light-subtle">
                        <h6 class="mb-3">Support Ticket Fallback</h6>
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">To</label>
                                <InputText @bind-Value="emailRoutingSettings.SupportTo" class="form-control" />
                            </div>
                            <div class="col-12 col-md-6">
                                <label class="form-label">Cc</label>
                                <InputText @bind-Value="emailRoutingSettings.SupportCc" class="form-control" />
                            </div>
                        </div>
                    </div>
                </div>

                <div class="d-flex gap-2 mt-4">
                    <button class="btn btn-primary" @onclick="SaveEmailRoutingSettings" disabled="@isSavingEmailRouting">
                        @(isSavingEmailRouting ? "Saving..." : "Save Email Routing")
                    </button>
                </div>

                @if (!string.IsNullOrWhiteSpace(emailRoutingStatusMessage))
                {
                    <div class="mt-3 fw-semibold @(emailRoutingSaveFailed ? "text-danger" : "text-success")">@emailRoutingStatusMessage</div>
                }
            </div>
        }

        @if (activeTab == "flappybird")
        {
            <div>
                <p class="text-muted mb-3">
                    Press <strong>Space</strong> to flap and fly through the pipe gaps. Tight gaps, one hit ends the run.
                </p>

                <div class="d-flex flex-wrap gap-4 align-items-start">
                    <div>
                        <canvas id="adminFlappyCanvas" width="520" height="600" class="border rounded bg-light"></canvas>
                        <div class="d-flex gap-2 mt-3">
                            <button class="btn btn-primary" @onclick="StartFlappyGame">Start / Restart</button>
                        </div>
                        @if (!string.IsNullOrWhiteSpace(flappyStatusMessage))
                        {
                            <div class="mt-2 text-danger small">@flappyStatusMessage</div>
                        }
                        <div class="mt-2 small text-muted">
                            Signed in as: <strong>@currentUserName</strong>
                        </div>
                        @if (lastFlappyScore > 0)
                        {
                            <div class="mt-2 fw-semibold">Last score: @lastFlappyScore</div>
                        }
                    </div>

                    <div class="border rounded p-3" style="min-width: 280px;">
                        <h6 class="mb-3">Leaderboards (Top 5)</h6>

                        @if (flappyLeaderboards == null)
                        {
                            <p class="text-muted mb-0">Loading scores...</p>
                        }
                        else
                        {
                            @RenderLeaderboard("Daily Leaderboard", flappyLeaderboards.Daily)
                            @RenderLeaderboard("Weekly Leaderboard", flappyLeaderboards.Weekly)
                            @RenderLeaderboard("Monthly Leaderboard", flappyLeaderboards.Monthly)
                            @RenderLeaderboard("All Time Leaderboard", flappyLeaderboards.AllTime, false)
                        }
                    </div>
                </div>
            </div>
        }
            </section>
        </div>
    }
</div>

<style>
    .admin-layout {
        display: grid;
        grid-template-columns: minmax(245px, 320px) minmax(0, 1fr);
        gap: 1rem;
    }

    .admin-tab-grid {
        display: grid;
        gap: .5rem;
    }

    .admin-tab-button {
        border: 1px solid #d7dde5;
        background: #fff;
        border-radius: .6rem;
        padding: .65rem .75rem;
        line-height: 1.2;
    }

    .admin-tab-button:hover {
        border-color: #8aa7cc;
        background: #f8fbff;
    }

    .admin-tab-button.active {
        border-color: #0d6efd;
        box-shadow: 0 0 0 .15rem rgba(13, 110, 253, .15);
        background: #eef5ff;
    }

    @@media (max-width: 992px) {
        .admin-layout {
            grid-template-columns: 1fr;
        }
    }
</style>

@code {
    private List<string>? logFiles;
    private string? logContent;
    private bool isAuthorized = false;
    private bool isMobile = false;
    private string activeTab = "logs";
    private string adminGroupId = "1f7897c7-a5b7-437c-9697-626c1e758f04";
    private List<ItNewsItem>? itNewsItems;
    private string newItNewsTitle = string.Empty;
    private string newItNewsContent = string.Empty;
    private DateTime? newItNewsExpirationDate;
    private bool newItNewsArchivable;
    private Guid? editingItNewsId;
    private string editItNewsTitle = string.Empty;
    private string editItNewsContent = string.Empty;
    private DateTime? editItNewsExpirationDate;
    private bool editItNewsArchivable;
    private bool showNewsSaveSuccess = false;
    private string? generatedPassword;
    private bool showCopySuccess = false;
    private FlappyLeaderboardSnapshot? flappyLeaderboards;
    private string currentUserName = "anonymous";
    private int lastFlappyScore = 0;
    private DotNetObjectReference<Admin>? flappyDotNetRef;
    private bool flappyInitialized = false;
    private string? flappyStatusMessage;
    private string signedInUserLogin = "anonymous";
    private List<SowDocumentInfo>? sowDocuments;
    private string? sowUploadStatus;
    private List<ItProjectTrackerItem>? itProjectItems;
    private string newProjectTitle = string.Empty;
    private string newProjectNotes = string.Empty;
    private DateTime? newProjectCompletedDate;
    private Guid? editingProjectId;
    private string editProjectTitle = string.Empty;
    private string editProjectNotes = string.Empty;
    private DateTime? editProjectCompletedDate;
    private bool showProjectSaveSuccess = false;
    private List<KnowledgeBaseFolderItem>? knowledgeBaseFolders;
    private string newKnowledgeFolderName = string.Empty;
    private Guid? editingKnowledgeFolderId;
    private string editKnowledgeFolderName = string.Empty;
    private string selectedFolderForNewArticleId = string.Empty;
    private string newKnowledgeArticleTitle = string.Empty;
    private string newKnowledgeArticleEmbedUrl = string.Empty;
    private Guid? editingKnowledgeFolderForArticleId;
    private Guid? editingKnowledgeArticleId;
    private string editKnowledgeArticleTitle = string.Empty;
    private string editKnowledgeArticleEmbedUrl = string.Empty;
    private string editKnowledgeArticleFolderId = string.Empty;
    private string? knowledgeBaseStatusMessage;
    private EmailRoutingSettings emailRoutingSettings = new();
    private bool isSavingEmailRouting = false;
    private bool emailRoutingSaveFailed = false;
    private string? emailRoutingStatusMessage;

    private void SetActiveTab(string tab)
    {
        activeTab = tab;

        if (tab == "flappybird")
        {
            flappyInitialized = false;
            flappyStatusMessage = null;
        }
    }

    private string IsActiveTab(string tab) => activeTab == tab ? "active" : "";

    private string GetTabHeading(string tab) => tab switch
    {
        "logs" => "System Logs",

        "calendar" => string.Empty,
        "itnews" => string.Empty,
        "sowdocs" => string.Empty,
        "mondayminute" => "Monday Minute Manager",
        "passwords" => string.Empty,
        "projecttracker" => string.Empty,
        "knowledgebase" => string.Empty,
        "emails" => string.Empty,
        "flappybird" => "Flappy Bird",
        _ => "Admin Dashboard"
    };

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var token = await TokenAcquisition.GetAccessTokenForUserAsync(new[] { "User.Read.All" });

            var authState = await AuthStateProvider.GetAuthenticationStateAsync();
            var user = authState.User;
            signedInUserLogin = user?.Identity?.Name ?? "anonymous";
            currentUserName = GetLeaderboardDisplayName(signedInUserLogin);

            if (user.Identity?.IsAuthenticated == true)
            {
                var userId = user.FindFirst("http://schemas.microsoft.com/identity/claims/objectidentifier")?.Value;

                if (!string.IsNullOrEmpty(userId))
                {
                    isAuthorized = await GraphUserService.IsUserInGroupAsync(userId, adminGroupId);
                }
            }

            if (isAuthorized)
            {
                logFiles = await Logger.GetLogFileNamesAsync();
                itNewsItems = await ItNewsService.LoadNewsAsync();
                flappyLeaderboards = await FlappyLeaderboardService.GetLeaderboardsAsync(5);
                sowDocuments = await SowDocumentService.ListDocumentsAsync();
                itProjectItems = await ItProjectTrackerService.LoadItemsAsync(signedInUserLogin);
                knowledgeBaseFolders = await KnowledgeBaseService.LoadFoldersAsync();
                emailRoutingSettings = await EmailRoutingSettingsService.GetSettingsAsync();
            }
        }
        catch (MsalUiRequiredException ex)
        {
            ConsentHandler.HandleException(ex);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Unexpected error during token acquisition or group check: {ex.Message}");
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var width = await JS.InvokeAsync<int>("blazorGetWindowWidth");
            isMobile = width < 768;
            StateHasChanged();
        }

        if (activeTab == "flappybird" && !flappyInitialized)
        {
            await InitializeFlappyGameAsync();
        }

        if (activeTab != "flappybird" && flappyInitialized)
        {
            await JS.InvokeVoidAsync("adminFlappyBird.dispose");
            flappyInitialized = false;
        }
    }

    private async Task<bool> InitializeFlappyGameAsync(bool force = false)
    {
        if (flappyInitialized && !force)
        {
            return true;
        }

        flappyDotNetRef?.Dispose();
        flappyDotNetRef = DotNetObjectReference.Create(this);
        flappyInitialized = await JS.InvokeAsync<bool>("adminFlappyBird.init", "adminFlappyCanvas", flappyDotNetRef);
        return flappyInitialized;
    }

    private async Task StartFlappyGame()
    {
        flappyStatusMessage = null;

        try
        {
            var initialized = await InitializeFlappyGameAsync();
            if (!initialized)
            {
                await Task.Delay(50);
                initialized = await InitializeFlappyGameAsync(force: true);
            }

            if (!initialized)
            {
                flappyStatusMessage = "Game canvas was not ready. Please try again.";
                return;
            }

            var started = await JS.InvokeAsync<bool>("adminFlappyBird.start");
            if (!started)
            {
                flappyInitialized = false;
                flappyStatusMessage = "Unable to start game. Please click Start again.";
            }
        }
        catch
        {
            flappyInitialized = false;
            flappyStatusMessage = "Unable to start game due to a script error. Please refresh and try again.";
        }
    }

    [JSInvokable]
    public async Task OnFlappyGameOver(int score)
    {
        lastFlappyScore = score;

        if (score > 0)
        {
            var currentTopScore = (await FlappyLeaderboardService.GetTopScoresAsync(1)).FirstOrDefault()?.Score ?? 0;
            var isNewHighScore = score > currentTopScore;

            await FlappyLeaderboardService.AddScoreAsync(currentUserName, score);
            flappyLeaderboards = await FlappyLeaderboardService.GetLeaderboardsAsync(5);

            if (isNewHighScore)
            {
                var fullUserName = await GetCurrentUserNameAsync();
                await Logger.LogActivityAsync(fullUserName, $"Set new Flappy Bird high score: {score}");
            }
        }

        await InvokeAsync(StateHasChanged);
    }

    private RenderFragment RenderLeaderboard(string title, List<FlappyScore> scores, bool addSpacing = true) => builder =>
    {
        var sequence = 0;

        builder.OpenElement(sequence++, "div");
        if (addSpacing)
        {
            builder.AddAttribute(sequence++, "class", "mb-3");
        }

        builder.OpenElement(sequence++, "h6");
        builder.AddAttribute(sequence++, "class", "mb-2");
        builder.AddContent(sequence++, title);
        builder.CloseElement();

        if (scores.Count == 0)
        {
            builder.OpenElement(sequence++, "p");
            builder.AddAttribute(sequence++, "class", "text-muted mb-0 small");
            builder.AddContent(sequence++, "No scores yet.");
            builder.CloseElement();
        }
        else
        {
            builder.OpenElement(sequence++, "ol");
            builder.AddAttribute(sequence++, "class", "mb-0 ps-3");

            foreach (var score in scores)
            {
                builder.OpenElement(sequence++, "li");
                builder.AddAttribute(sequence++, "class", "mb-1");

                builder.OpenElement(sequence++, "span");
                builder.AddAttribute(sequence++, "class", "fw-semibold");
                builder.AddContent(sequence++, score.UserName);
                builder.CloseElement();

                builder.AddContent(sequence++, $" — {score.Score}");
                builder.CloseElement();
            }

            builder.CloseElement();
        }

        builder.CloseElement();
    };

    private async Task OnLogSelected(ChangeEventArgs e)
    {
        var selectedFile = e.Value?.ToString();
        if (!string.IsNullOrEmpty(selectedFile))
        {
            logContent = await Logger.ReadLogContentAsync(selectedFile);
        }
    }

    private async Task AddNewsItem()
    {
        if (itNewsItems == null || string.IsNullOrWhiteSpace(newItNewsTitle) || string.IsNullOrWhiteSpace(newItNewsContent))
            return;

        var itemTitle = newItNewsTitle.Trim();

        itNewsItems.Add(new ItNewsItem
        {
            Id = Guid.NewGuid(),
            Title = itemTitle,
            Content = newItNewsContent.Trim(),
            PostedOn = DateTime.UtcNow,
            ExpirationDate = newItNewsExpirationDate?.Date,
            Archivable = newItNewsArchivable
        });

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Added IT News Item {itemTitle}");

        newItNewsTitle = string.Empty;
        newItNewsContent = string.Empty;
        newItNewsExpirationDate = null;
        newItNewsArchivable = false;
        await SaveNewsAndShowStatus();
    }

    private void StartEditNewsItem(ItNewsItem item)
    {
        editingItNewsId = item.Id;
        editItNewsTitle = item.Title;
        editItNewsContent = item.Content;
        editItNewsExpirationDate = item.ExpirationDate?.Date;
        editItNewsArchivable = item.Archivable;
    }

    private void CancelEditNewsItem()
    {
        editingItNewsId = null;
        editItNewsTitle = string.Empty;
        editItNewsContent = string.Empty;
        editItNewsExpirationDate = null;
        editItNewsArchivable = false;
    }

    private async Task SaveEditedNewsItem()
    {
        if (itNewsItems == null || editingItNewsId == null || string.IsNullOrWhiteSpace(editItNewsTitle) || string.IsNullOrWhiteSpace(editItNewsContent))
            return;

        var item = itNewsItems.FirstOrDefault(x => x.Id == editingItNewsId.Value);
        if (item == null)
            return;

        item.Title = editItNewsTitle.Trim();
        item.Content = editItNewsContent.Trim();
        item.ExpirationDate = editItNewsExpirationDate?.Date;
        item.Archivable = editItNewsArchivable;

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Edited IT News Item {item.Title}");

        await SaveNewsAndShowStatus();
        CancelEditNewsItem();
    }

    private async Task DeleteNewsItem(ItNewsItem item)
    {
        if (itNewsItems == null)
            return;

        var itemTitle = item.Title;

        itNewsItems.Remove(item);
        if (editingItNewsId == item.Id)
        {
            CancelEditNewsItem();
        }

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Deleted IT News Item {itemTitle}");

        await SaveNewsAndShowStatus();
    }

    private async Task SaveNewsAndShowStatus()
    {
        if (itNewsItems == null)
            return;

        await ItNewsService.SaveNewsAsync(itNewsItems);
        itNewsItems = await ItNewsService.LoadNewsAsync();

        showNewsSaveSuccess = true;
        StateHasChanged();

        await Task.Delay(2500);
        showNewsSaveSuccess = false;
        StateHasChanged();
    }


    private async Task HandleSowFileUpload(InputFileChangeEventArgs e)
    {
        var file = e.File;

        if (file is null)
        {
            return;
        }

        if (!file.Name.EndsWith(".pdf", StringComparison.OrdinalIgnoreCase) ||
            !string.Equals(file.ContentType, "application/pdf", StringComparison.OrdinalIgnoreCase))
        {
            sowUploadStatus = "Only PDF uploads are allowed.";
            return;
        }

        try
        {
            sowUploadStatus = "Uploading...";
            await SowDocumentService.UploadDocumentAsync(file);

            var userName = await GetCurrentUserNameAsync();
            await Logger.LogActivityAsync(userName, $"Uploaded SOW document {file.Name}");

            sowDocuments = await SowDocumentService.ListDocumentsAsync();
            sowUploadStatus = "Upload successful!";

            await Task.Delay(2000);
            sowUploadStatus = null;
        }
        catch (Exception ex)
        {
            sowUploadStatus = $"Upload failed: {ex.Message}";
        }
    }

    private async Task DeleteSowDocument(SowDocumentInfo document)
    {
        await SowDocumentService.DeleteDocumentAsync(document.FileName);

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Deleted SOW document {document.FileName}");

        sowDocuments = await SowDocumentService.ListDocumentsAsync();
    }

    private async Task<string> GetCurrentUserNameAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        return authState.User?.Identity?.Name ?? "anonymous";
    }


    private static string GetLeaderboardDisplayName(string? identityName)
    {
        if (string.IsNullOrWhiteSpace(identityName))
        {
            return "anonymous";
        }

        var emailLocalPart = identityName.Split('@')[0];
        if (string.IsNullOrWhiteSpace(emailLocalPart))
        {
            return "anonymous";
        }

        var firstName = emailLocalPart.Split('.')[0];
        return string.IsNullOrWhiteSpace(firstName) ? "anonymous" : firstName.Trim().ToLowerInvariant();
    }

    private async Task AddProjectItem()
    {
        if (itProjectItems == null || string.IsNullOrWhiteSpace(newProjectTitle))
            return;

        var trimmedTitle = newProjectTitle.Trim();

        itProjectItems.Add(new ItProjectTrackerItem
        {
            Id = Guid.NewGuid(),
            Title = trimmedTitle,
            Notes = newProjectNotes.Trim(),
            CompletedDate = newProjectCompletedDate,
            LastUpdatedOn = DateTime.UtcNow
        });

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Added IT project tracker item {trimmedTitle}");

        newProjectTitle = string.Empty;
        newProjectNotes = string.Empty;
        newProjectCompletedDate = null;

        await SaveProjectsAndShowStatus();
    }

    private void StartEditProjectItem(ItProjectTrackerItem item)
    {
        editingProjectId = item.Id;
        editProjectTitle = item.Title;
        editProjectNotes = item.Notes;
        editProjectCompletedDate = item.CompletedDate;
    }

    private void CancelEditProjectItem()
    {
        editingProjectId = null;
        editProjectTitle = string.Empty;
        editProjectNotes = string.Empty;
        editProjectCompletedDate = null;
    }

    private async Task SaveEditedProjectItem()
    {
        if (itProjectItems == null || editingProjectId == null || string.IsNullOrWhiteSpace(editProjectTitle))
            return;

        var item = itProjectItems.FirstOrDefault(x => x.Id == editingProjectId.Value);
        if (item == null)
            return;

        item.Title = editProjectTitle.Trim();
        item.Notes = editProjectNotes.Trim();
        item.CompletedDate = editProjectCompletedDate;
        item.LastUpdatedOn = DateTime.UtcNow;

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Edited IT project tracker item {item.Title}");

        await SaveProjectsAndShowStatus();
        CancelEditProjectItem();
    }

    private async Task DeleteProjectItem(ItProjectTrackerItem item)
    {
        if (itProjectItems == null)
            return;

        var itemTitle = item.Title;
        itProjectItems.Remove(item);

        if (editingProjectId == item.Id)
        {
            CancelEditProjectItem();
        }

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Deleted IT project tracker item {itemTitle}");

        await SaveProjectsAndShowStatus();
    }

    private async Task SaveProjectsAndShowStatus()
    {
        if (itProjectItems == null)
            return;

        await ItProjectTrackerService.SaveItemsAsync(signedInUserLogin, itProjectItems);
        itProjectItems = await ItProjectTrackerService.LoadItemsAsync(signedInUserLogin);

        showProjectSaveSuccess = true;
        StateHasChanged();

        await Task.Delay(2500);
        showProjectSaveSuccess = false;
        StateHasChanged();
    }

    private async Task AddKnowledgeFolder()
    {
        if (knowledgeBaseFolders == null || string.IsNullOrWhiteSpace(newKnowledgeFolderName))
            return;

        var folderName = newKnowledgeFolderName.Trim();
        knowledgeBaseFolders.Add(new KnowledgeBaseFolderItem
        {
            Id = Guid.NewGuid(),
            Name = folderName,
            CreatedOn = DateTime.UtcNow,
            Articles = new List<KnowledgeBaseArticleItem>()
        });

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Added Knowledge Base folder {folderName}");

        newKnowledgeFolderName = string.Empty;
        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
    }

    private void StartEditKnowledgeFolder(KnowledgeBaseFolderItem folder)
    {
        editingKnowledgeFolderId = folder.Id;
        editKnowledgeFolderName = folder.Name;
    }

    private void CancelEditKnowledgeFolder()
    {
        editingKnowledgeFolderId = null;
        editKnowledgeFolderName = string.Empty;
    }

    private async Task SaveEditedKnowledgeFolder()
    {
        if (knowledgeBaseFolders == null || editingKnowledgeFolderId == null || string.IsNullOrWhiteSpace(editKnowledgeFolderName))
            return;

        var folder = knowledgeBaseFolders.FirstOrDefault(x => x.Id == editingKnowledgeFolderId.Value);
        if (folder == null)
            return;

        folder.Name = editKnowledgeFolderName.Trim();

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Edited Knowledge Base folder {folder.Name}");

        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
        CancelEditKnowledgeFolder();
    }

    private async Task DeleteKnowledgeFolder(KnowledgeBaseFolderItem folder)
    {
        if (knowledgeBaseFolders == null)
            return;

        var folderName = folder.Name;
        knowledgeBaseFolders.Remove(folder);

        if (editingKnowledgeFolderId == folder.Id)
        {
            CancelEditKnowledgeFolder();
        }

        if (selectedFolderForNewArticleId == folder.Id.ToString())
        {
            selectedFolderForNewArticleId = string.Empty;
        }

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Deleted Knowledge Base folder {folderName}");

        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
    }

    private async Task AddKnowledgeArticle()
    {
        if (knowledgeBaseFolders == null ||
            string.IsNullOrWhiteSpace(selectedFolderForNewArticleId) ||
            string.IsNullOrWhiteSpace(newKnowledgeArticleTitle) ||
            string.IsNullOrWhiteSpace(newKnowledgeArticleEmbedUrl))
        {
            return;
        }

        if (!Guid.TryParse(selectedFolderForNewArticleId, out var folderId))
            return;

        var folder = knowledgeBaseFolders.FirstOrDefault(x => x.Id == folderId);
        if (folder == null)
            return;

        var articleTitle = newKnowledgeArticleTitle.Trim();
        var embedUrl = NormalizeScribeEmbedUrl(newKnowledgeArticleEmbedUrl);
        if (string.IsNullOrWhiteSpace(embedUrl))
            return;

        folder.Articles.Add(new KnowledgeBaseArticleItem
        {
            Id = Guid.NewGuid(),
            Title = articleTitle,
            EmbedUrl = embedUrl,
            CreatedOn = DateTime.UtcNow
        });

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Added Knowledge Base article {articleTitle} to folder {folder.Name}");

        newKnowledgeArticleTitle = string.Empty;
        newKnowledgeArticleEmbedUrl = string.Empty;

        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
    }

    private void StartEditKnowledgeArticle(KnowledgeBaseFolderItem folder, KnowledgeBaseArticleItem article)
    {
        editingKnowledgeFolderForArticleId = folder.Id;
        editingKnowledgeArticleId = article.Id;
        editKnowledgeArticleTitle = article.Title;
        editKnowledgeArticleEmbedUrl = article.EmbedUrl;
        editKnowledgeArticleFolderId = folder.Id.ToString();
    }

    private void CancelEditKnowledgeArticle()
    {
        editingKnowledgeFolderForArticleId = null;
        editingKnowledgeArticleId = null;
        editKnowledgeArticleTitle = string.Empty;
        editKnowledgeArticleEmbedUrl = string.Empty;
        editKnowledgeArticleFolderId = string.Empty;
    }

    private async Task SaveEditedKnowledgeArticle()
    {
        if (knowledgeBaseFolders == null || editingKnowledgeFolderForArticleId == null || editingKnowledgeArticleId == null ||
            string.IsNullOrWhiteSpace(editKnowledgeArticleTitle) || string.IsNullOrWhiteSpace(editKnowledgeArticleEmbedUrl) ||
            !Guid.TryParse(editKnowledgeArticleFolderId, out var targetFolderId))
            return;

        var sourceFolder = knowledgeBaseFolders.FirstOrDefault(x => x.Id == editingKnowledgeFolderForArticleId.Value);
        var targetFolder = knowledgeBaseFolders.FirstOrDefault(x => x.Id == targetFolderId);
        var article = sourceFolder?.Articles.FirstOrDefault(x => x.Id == editingKnowledgeArticleId.Value);
        if (article == null || sourceFolder == null || targetFolder == null)
            return;

        article.Title = editKnowledgeArticleTitle.Trim();
        article.EmbedUrl = NormalizeScribeEmbedUrl(editKnowledgeArticleEmbedUrl) ?? article.EmbedUrl;

        if (sourceFolder.Id != targetFolder.Id)
        {
            sourceFolder.Articles.Remove(article);
            targetFolder.Articles.Add(article);
        }

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Edited Knowledge Base article {article.Title} in folder {targetFolder.Name}");

        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
        CancelEditKnowledgeArticle();
    }

    private async Task DeleteKnowledgeArticle(KnowledgeBaseFolderItem folder, KnowledgeBaseArticleItem article)
    {
        if (knowledgeBaseFolders == null)
            return;

        var articleTitle = article.Title;
        folder.Articles.Remove(article);

        if (editingKnowledgeArticleId == article.Id)
        {
            CancelEditKnowledgeArticle();
        }

        var userName = await GetCurrentUserNameAsync();
        await Logger.LogActivityAsync(userName, $"Deleted Knowledge Base article {articleTitle} from folder {folder.Name}");

        await SaveKnowledgeBaseAndShowStatus("Knowledge base saved!");
    }

    private async Task SaveKnowledgeBaseAndShowStatus(string message)
    {
        if (knowledgeBaseFolders == null)
            return;

        await KnowledgeBaseService.SaveFoldersAsync(knowledgeBaseFolders);
        knowledgeBaseFolders = await KnowledgeBaseService.LoadFoldersAsync();

        knowledgeBaseStatusMessage = message;
        StateHasChanged();

        await Task.Delay(2500);
        knowledgeBaseStatusMessage = null;
        StateHasChanged();
    }

    private async Task SaveEmailRoutingSettings()
    {
        isSavingEmailRouting = true;
        emailRoutingSaveFailed = false;
        emailRoutingStatusMessage = null;

        try
        {
            await EmailRoutingSettingsService.SaveSettingsAsync(emailRoutingSettings);
            emailRoutingSettings = await EmailRoutingSettingsService.GetSettingsAsync();

            var userName = await GetCurrentUserNameAsync();
            await Logger.LogActivityAsync(userName, "Updated email routing settings");

            emailRoutingStatusMessage = "Email routing saved.";
        }
        catch (Exception ex)
        {
            emailRoutingSaveFailed = true;
            emailRoutingStatusMessage = $"Unable to save email routing: {ex.Message}";
        }
        finally
        {
            isSavingEmailRouting = false;
        }
    }

    private static string? NormalizeScribeEmbedUrl(string? input)
    {
        if (string.IsNullOrWhiteSpace(input))
        {
            return null;
        }

        var trimmed = input.Trim();

        if (trimmed.Contains("<iframe", StringComparison.OrdinalIgnoreCase))
        {
            var srcMatch = System.Text.RegularExpressions.Regex.Match(trimmed, "src\\s*=\\s*\"(?<url>[^\"]+)\"", System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (srcMatch.Success)
            {
                trimmed = srcMatch.Groups["url"].Value;
            }
        }

        return trimmed;
    }

    private void GeneratePassword()
    {
        generatedPassword = PasswordGenerator.GeneratePassword();
        showCopySuccess = false;
    }

    private async Task CopyPassword()
    {
        if (string.IsNullOrWhiteSpace(generatedPassword))
            return;

        await JS.InvokeVoidAsync("blazorCopyToClipboard", generatedPassword);

        showCopySuccess = true;
        StateHasChanged();

        await Task.Delay(1500);
        showCopySuccess = false;
        StateHasChanged();
    }

    public async ValueTask DisposeAsync()
    {
        try
        {
            await JS.InvokeVoidAsync("adminFlappyBird.dispose");
        }
        catch
        {
            // ignore dispose JS errors during shutdown
        }

        flappyDotNetRef?.Dispose();
    }
}
